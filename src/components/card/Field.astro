---
interface Props {
  label: string;
  value: string | undefined;
}

const { label, value } = Astro.props;
---

{
  value === undefined ? undefined : (
    <div>
      {label}: {value}
    </div>
  )
}
